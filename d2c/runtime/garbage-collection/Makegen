$COPTS = "$COPTS $GC_CFLAGS -I/usr/include/gc";

$D2CFLAGS = '-L../dylan -L../melange';

&emit_library_rule('garbage-collection', '$(BUILDROOT)/force.timestamp', '',
		   'compile', 'install', 'no-mindy');
